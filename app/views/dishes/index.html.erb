<div class="container py-4">
  <div class="row justify-content-center">
    <div class="col-12 col-md-6">
      <div class="card text-center shadow position-relative" style="min-height: 600px;">
        <div class="card-body d-flex flex-column flex-grow-1">

          <div class="flex-grow-1 d-flex flex-column justify-content-center">
            <h1 class="text-danger mb-4">🍜 今日食べたい中華料理は？</h1>

            <% if @dish %>
              <h2 class="text-warning h4 mb-3"><%= @dish[:name] %></h2>
              <%= image_tag @dish[:image], alt: @dish[:name], class: "img-fluid fixed-img mb-4 mx-auto", style: "max-height: 200px; object-fit: cover;" %>
            <% else %>
              <p>料理が見つかりませんでした。</p>
            <% end %>
          </div>

          <div class="position-absolute bottom-0 start-50 translate-middle-x mb-3">
            <%= button_to "料理を決める！", generate_path, method: :get, class: "btn btn-danger btn-lg" %>
          </div>

        </div>
      </div>
    </div>
  </div>
</div>